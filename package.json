{
  "name": "maple-potential-calculator",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild entry.jsx --bundle --minify --loader:.jsx=jsx --jsx=automatic --define:process.env.NODE_ENV=\"\\\"production\\\"\" --outfile=bundle.js && node make_html.mjs"
  },
  "dependencies": { "react": "^18.3.1", "react-dom": "^18.3.1" },
  "devDependencies": { "esbuild": "^0.24.0" }
}
